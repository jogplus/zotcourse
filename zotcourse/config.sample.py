class Config:
    WEBSOC_BASE_URL = "WEBSOC_BASE_URL"
    GRADES_FILE_NAME = "GRADES_FILE_NAME"
    # Time (seconds) until cache will expire for form and listing
    LISTING_EXPIRE_TIME = 60 * 60 * 24
    IGNORE_TIME = -1
    COURSE_CODE_LIMIT = 10
